services:
  - type: web
    name: vayudrishti-weather-platform
    env: python
    buildCommand: pip install -r requirements.txt websockets
    startCommand: uvicorn backend.app.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.8
